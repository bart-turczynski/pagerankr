% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_urls.R
\name{resolve_urls}
\alias{resolve_urls}
\title{Resolve URLs Through Redirects}
\usage{
resolve_urls(
  urls,
  redirects_df,
  redirect_from_col = "from",
  redirect_to_col = "to",
  duplicate_from_policy = c("strict", "first_wins", "last_wins", "most_frequent",
    "prune_source", "resolve_if_consistent"),
  loop_handling = c("error", "prune_loop", "break_arrow")
)
}
\arguments{
\item{urls}{Character vector of URLs to resolve.}

\item{redirects_df}{A data frame containing redirect rules.}

\item{redirect_from_col}{Character, name of the source column. Default
\code{"from"}.}

\item{redirect_to_col}{Character, name of the target column. Default
\code{"to"}.}

\item{duplicate_from_policy}{How to handle conflicting redirects. Passed
through to redirect preprocessing. Default \code{"strict"}.}

\item{loop_handling}{How to handle redirect cycles. Default \code{"error"}.
Use \code{"prune_loop"} or \code{"break_arrow"} to resolve despite loops.}
}
\value{
A data frame with columns:
  \describe{
    \item{original}{The input URL.}
    \item{resolved}{The final destination after following all redirects.}
    \item{changed}{Logical, whether the URL was modified by a redirect.}
  }
}
\description{
Given a character vector of URLs and a redirect data frame,
  resolves each URL to its final destination by following redirect chains.
  Unlike \code{\link{resolve_redirects}}, this function does not require an
  edge list -- it works directly on a list of URLs. This helper is
  redirect-only; use [resolve_canonical_urls()] for `rel=canonical` folding
  or [resolve_folded_urls()] for composed redirect plus canonical folding.
}
\examples{
redirects <- data.frame(
  from = c("A", "B", "C"),
  to = c("B", "C", "Final")
)

# Resolve specific URLs
resolve_urls(c("A", "B", "X"), redirects)

# X is not in the redirect map, so it stays as-is
}
